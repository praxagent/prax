# Adopt Tracker — concrete things to bring into Prax, and where each stands

**Purpose.** The research lane (`docs/research/`) produces a verdict per assessment, but the *actionable* "adopt X" candidates scatter across ~30 docs and the backlog. This is the single place that tracks them so none is lost. It is a **living index of decisions**, not new analysis — each row points at the assessment that argued it and the code/backlog item that will carry it.

**Started:** 2026-07-13 (TJ: "track other things we should bring in so we don't lose track"). Seeded from the recent assessment cluster; extend it whenever an assessment lands an adopt-candidate.

**Status legend:** ✅ shipped · 🔨 in PR / in progress · 📋 queued (agreed, not started) · ⏸ parked (blocked on a decision or a precondition) · 💤 someday (needs infra Prax doesn't have)

---

## The self-regeneration cluster (highest leverage — this is the #29 through-line)

Everything here feeds [IDEAS_BACKLOG #29](../IDEAS_BACKLOG.md) (close the recursive-self-improvement loop inward). The gate is an un-gameable fitness function; these rows build it.

| Item | From | Status | Where it lands |
|---|---|---|---|
| **Constrained evolution: only task-agnostic patches may be PROPOSED** (a global constraint on the loop's output, not a review check afterwards — restrict modifications to changes that generalize beyond the tasks that motivated them) | [harness-compass](harness-compass.md) | 📋 | **our anti-spike rule as a precondition rather than a policy.** With [Weng's](weng-harness-engineering.md) read-only scorer and [R1's](harness-r1.md) frozen target this completes #29 P1's spec: scorer read-only, agent frozen, patch task-agnostic — anything else rejected before it is scored. Third independent justification for the anti-spike rule (safety + epistemics + measured performance) |
| **Component-wise optimization, then verify the consolidated config** (optimizing interacting components jointly causes interference; decouple, improve, then confirm the combination beats each part) | [harness-compass](harness-compass.md) | 📋 | concrete extension to the [flag campaign](flag-eval-campaign-2026-07-08.md): we A/B one flag at a time against a fixed baseline, which cannot catch two flags that are individually neutral and jointly harmful. ~30 interacting flags makes this real, not hypothetical |
| **Reward = rerun-the-same-tasks delta, never a judge** (apply the proposed change, rerun the IDENTICAL tasks against the frozen agent, and the reward IS the measured difference — no rubric to talk around) | [harness-r1](harness-r1.md) | 📋 | the un-gameable fitness function #29 has been circling, with an external existence proof. `accept_change` already encodes the accept rule and has **zero consumers** — this is the loop that should consume it |
| **Freeze the target, edit only the harness** (the agent under improvement never changes; only the scaffold does) | [harness-r1](harness-r1.md) | 📋 | bounds the blast radius of any self-proposed change and makes regressions revertible by dropping a patch. With [Weng's](weng-harness-engineering.md) read-only scorer this gives #29 P1 **two invariants**: the scorer is read-only, the agent is frozen, and the loop may write exactly one surface |
| **Constrained edit surface: named lifecycle hooks, not free-form code** (`on_init` / pre-hint / `on_before_action` allow-block-rewrite-force / `on_post_step`) | [harness-r1](harness-r1.md) | 📋 | what makes patches validatable, sandboxable and revertible. Prax already has the seams — `loop_middleware.py` and `governed_tool.py` (which IS `on_before_action`) — so P1 should edit **existing middleware hooks**, turning "only edit safe things" from policy into property |
| **Make the scorer READ-ONLY to the self-regen loop** (mount goldens, `prax/eval/`, the tracer and the trace store read-only during a #29 run, so "improve the score by editing the scorer" is UNREACHABLE rather than forbidden) | [weng](weng-harness-engineering.md) | 📋 | converts our existing *policy* row ("eval-harness edits = HIGH-risk/human-gated") into a *property*. Same instinct as the secrets proxy: a compromised loop has nothing to reach because it isn't mounted, not because a rule says don't. Cheap; do it before #29 P1 runs unattended |
| **Value long-term code health in the accept-gate** (maintainability, ownership boundaries, migration cost, backwards compatibility — a change that passes goldens at lower cost can still be a maintenance disaster) | [weng](weng-harness-engineering.md) | 📋 | **the one bottleneck of Weng's seven we had NO row for.** Same horizon problem as the [Asari](asari-inference-optimization.md) behaviour-preserving split and the [weakest-not-shortest](weakest-not-shortest.md) correction — three independent sources now say the accept-gate optimises the wrong horizon |
| **Equivalence testing for behaviour-PRESERVING changes** (split the accept-gate in two: a *fix* is judged on goldens; a *refactor/optimisation/simplification* must produce **unchanged outputs** on a corpus — and a refactor that IMPROVES the golden score has changed behaviour and needs scrutiny, not applause) | [asari](asari-inference-optimization.md) | 📋 | closes a real hole: `/simplify`-style changes ([weakest-not-shortest](weakest-not-shortest.md)) are exactly this class and get no equivalence check today. Also the sharper technique the [FailureAtlas](failure-atlas-assessment.md) proxy-silent-failure row was missing — assert the proxied response equals the un-proxied one, not that it returned 200 |
| **"How narrow is the trigger?" — weakness, not brevity, as the generality test** (a spike is a STRONG hypothesis: tiny extension, fires only on the failing case; a general fix is WEAK. Ask over how wide a class of inputs a change alters behaviour, and treat a narrow trigger as a suspected spike regardless of how clean it looks) | [weakest-not-shortest](weakest-not-shortest.md) | 📋 | an auditable question for the #29 accept-gate and `/code-review`, requiring no computation — **weakness is NOT computable for a code diff**, so this is a lens, never a score. Supplies the epistemic argument for the anti-spike rule that has so far rested on safety alone |
| **Calibrated self-assessment: state a probability, then score it** (completion claims carry a confidence in [0,1]; the hidden verifier/golden resolves it → a Brier score on whether Prax KNOWS it succeeded, distinct from whether it succeeds) | [futuresearch](futuresearch-calibrated-forecasting.md) + [crux](crux-shadow-evals.md) | 🔧 shipped in `tb_agent` | Motivated by measurement: **85% of Terminal-Bench scored failures claimed success**. Grounded (verifier-resolved, not rubric-resolved) so it can't be gamed by rewriting the rubric. **Unscored confidence is decoration** — wire the Brier scoring, then extend the pattern to `agent_step_done` and any other self-reported completion |
| **Decision forecasts before committing to an approach** ("if I do X, will I achieve this outcome?" — forecast, act, compare; the gap drives replan-vs-persist) | [futuresearch](futuresearch-calibrated-forecasting.md) | 📋 | the front half of the failure-provenance row; distinct from the world-model row (that induces mechanics, this estimates outcomes) |
| **Traces as a versioned protocol carrying per-step outcome signal** (schema version + optional per-step outcome populated from whatever verdict exists: golden pass/fail, `claim_audit`, user rejection, task-runner result — today a trace records what happened but never whether it was good, which makes it an audit log rather than a learning substrate) | [agentic-rl](agentic-rl-evolution-control-plane.md) | 📋 | cheap + additive to `trace.py`, and the **precondition for three rows already agreed**: trace clustering → eval mining, the grounded signal, failure-provenance diagnosis. Build the substrate now even though the decision layer is far off |
| **Reuse/consent labelling on captured traces** (a retention/learnable marker set at capture time from channel + workspace policy, so a future self-improvement loop can filter to trajectories it is ALLOWED to learn from without re-litigating consent per use) | [agentic-rl](agentic-rl-evolution-control-plane.md) | 📋 | governance-first applied to Prax's own training data; on the live box (real Discord/SMS) this is a legal question, not only a technical one |
| **#29's decision layer: retrain / re-scaffold / simplify** (an evolution control plane picking the KIND of change from trajectory statistics — #29 currently assumes scaffold-editing is the only move) | [agentic-rl](agentic-rl-evolution-control-plane.md) + [harness-survey](agent-harness-engineering-survey.md) | ⏸ | bank the framing, don't build: at n=1 live box the statistics are too thin to decide anything. Its removal arm is the **adaptive simplification** row below |
| **AgentENV for sandbox freeze/resume + per-tenant microVMs** (Firecracker snapshots, <50ms resume, fork for parallel eval, E2B-compatible seam onto `prax_sandbox_client`) | [agentenv](agentenv-microvm-sandboxes.md) | ⏸ | Blocked on `/dev/kvm` — needs bare-metal. Written down as the evaluate-FIRST candidate when multi-tenant/k8s buys metal, instead of hand-building scale-to-zero |
| **Trace clustering → eval-case mining** (UMAP+HDBSCAN over the `prax_trace_summaries` embeddings that already exist; query-free — surfaces the failure theme nobody knew to search for; clusters of real-usage failures become golden candidates = a test distribution authored by reality) | [mastra](mastra-trace-intelligence.md) | 📋 | CPU-cheap batch over stored data, not infrastructure. Feeds the grounded-signal row below and the ARTS failure-provenance pattern; also ranks fixes by usage mass instead of report order |
| **Grounded signal alongside the authored gate** (the #29 gate is authored-with-hidden-instances; under rising optimization pressure hiding is not a defence — add signals from counterparties we don't control: real-usage acceptance/rejection of self-proposed changes, live-box outcomes, cost repricing) | [ilands](ilands-grounding-gap.md) | 📋 | The lens, not the marketplace. TJ's daily-driver loop IS grounded selection at n=1 — name it in the #29 design and wire it in as gate input, don't rebuild it as an economy |
| **Pre-registered kill conditions** (write the refutation condition down BEFORE an eval-gate run; the flag campaign already rejects on evidence — this is one paragraph more discipline) | [ilands](ilands-grounding-gap.md) | 📋 | Costs a paragraph per experiment; makes "we would have accepted X as disproof" checkable after the fact |
| **Public/private golden split** (select on held-out score) | [aide2](aide2-recursive-self-improvement.md) | 🔨 PR #80 | `Golden.visibility`, `summarize_split`, `accept_change` in `prax/eval/goldens.py` |
| **`accept_change` selection gate** (private↑ at ≤cost, fail-closed, reward-hack hard-reject) | [aide2](aide2-recursive-self-improvement.md) | 🔨 PR #80 | shipped as a pure helper; **no loop consumes it yet** |
| **Cost-budgeted selection** (improve private at equal-or-lower tokens) | [aide2](aide2-recursive-self-improvement.md) | 📋 | `accept_change` takes cost args; **wire real token metering** through `run_golden_suite`/replay |
| **Heterogeneous golden gate** (never gate on one golden = never reward a spike) | [aide2](aide2-recursive-self-improvement.md) | 📋 | #29 P1 design: gate a proposed change against the *whole* private set |
| **Complexity/dead-code gate on self-mods** (score↑ is necessary, not sufficient) | [aide2](aide2-recursive-self-improvement.md) — **corrected by** [weakest-not-shortest](weakest-not-shortest.md) | 📋 | #29 accept gate runs `/simplify` + syntax/layer linters before adopt. **Do NOT let this stand in for generality**: it rewards shortness, and a narrow special-case is short, lints clean and passes the golden — i.e. the gate can prefer the spike. Pair it with the weakness question below |
| **Eval-harness edits = HIGH-risk / human-gated** (the loop can reach its own scorer) | [aide2](aide2-recursive-self-improvement.md) | 📋 | #29 graded-autonomy boundary (policy, cheap to state) |
| **Taste signal in the accept-gate** (pairwise "is the new version *better*, not just passing?" preference judge — the missing *measurable* quality axis beyond correctness+simplicity) | [proofjudge](proofjudge-taste.md) | 📋 | #29 accept-gate: correctness (verify) + simplicity (complexity gate) + **taste** (pairwise-preference judge). Stops evolving correct-but-bloated code. Signal not hard-gate (~66% aligned); name whose taste |
| **Pairwise/context-aware judging** (rejected-vs-accepted preference + tools to judge *fit*, not isolated correctness; "ties ⇒ fix the rubric") | [proofjudge](proofjudge-taste.md) | 📋 | strengthens `score_golden` / the [diffuse-ai-control](diffuse-ai-control-judge-robustness.md) auditor where quality is subjective |
| **Audit the *spec/check*, not just the answer** (any flow where Prax writes BOTH a check and the thing checked — self-written test, self-graded rubric, self-formalized spec — the check is the un-verified surface; maker≠checker applied to the spec) | [axiomprover](axiomprover-imo-formalization.md) | 📋 | general auditing principle; extends `claim_audit`. Un-gameable ensembles = members filtered by a *sound* verifier (kernel), not an LLM judge |
| **RSI ladder framing** (delegation→net-positive→ignition→inflection) | [aide2](aide2-recursive-self-improvement.md) | ✅ | in the aide2 doc + #29 |
| **Disagreement-driven golden curation** (auditor↔judge disagreement = mislabel) | [expert-judgment](expert-judgment-finetune.md) | ✅ | `run_golden_curation` in `prax/eval/goldens.py` |
| **Supervising auditor** (high-tier re-checks the cheap judge's passes) | [diffuse-ai-control](diffuse-ai-control-judge-robustness.md) | ✅ | `score_golden(audit=…)`, `EVAL_AUDITOR_ENABLED` (#26) |
| **Substance-over-polish hardened judge prompt** | [diffuse-ai-control](diffuse-ai-control-judge-robustness.md) | ✅ | `_SCORE_PROMPT` |
| **Binary per-criterion judging (not Likert)** | [edge-bench](edge-bench-learning-curves.md) | ✅ | `_binarize` in `score_golden` |
| **"Verifiable beats judgeable"** (deterministic regex criteria) | [edge-bench](edge-bench-learning-curves.md) | ✅ | `RubricCriterion.verify` |
| **P1 plugin micro-loop** (notice→propose→isolate→verify→canary→rollback) | [autoresearch](autoresearch-labless.md) + [aide2](aide2-recursive-self-improvement.md) | ⏸ TJ | #29 P1 — de-risked by aide2 evidence; ~3–5 days; **gate ready after the split lands** |
| **Failure-provenance diagnosis** (classify bad-plan vs bad-execution before retry → replan vs retry) | [arts](arts-agentic-tree-search.md) | 📋 | flag-gated orchestrator/#29 heuristic (more surgical than auto-tier-escalation) + an eval-scoring lens ("don't punish a correct plan for flaky execution") |
| **Learnable-vs-noise failure prioritization** (don't burn #29 iterations on *irreducible* failures — flakiness/ambiguity = eval-form "noisy TV"; prioritize failures with learnable structure) | [learnable-novelty](learnable-novelty.md) | 📋 | sharpens failure-provenance-diagnosis; the scorecard health-guard (excludes infra failures) is the crude first version of this filter |
| **Significance testing before an eval "win"** (empirical-null from repeated baseline runs + best-of-N bootstrap p-value + Holm/majority-win correction — "real improvement or run-to-run noise?") | [harness-generalization](harness-generalization.md) | 📋 | highest-confidence adopt; a keyless `prax/eval/` helper the flag-eval A/Bs + self-regen accept-gate call before adopting a change. Makes the prime-directive "audit the measurement" quantitative. **Validated in practice 2026-08-02** ([retained-reasoning A/B](retained-reasoning-ab-2026-08-02.md)): a single run showed +1 case and −28% tokens; 3 runs per arm showed pass-rate means IDENTICAL — the win was noise. Without replication it would have shipped as a result. Case study for why: CRUX §4.9 — Fable 5 asked to fix a scaffold from pilot logs hill-climbed n=1 anecdotes ([crux](crux-shadow-evals.md)) |
| **Adaptive config allocation** (treat harness/config as a problem-dependent hyperparameter picked online from early partial-run signals; prune laggards + reallocate budget — Successive-Halving/ASHA `12→5→2→1`, budget-neutral) | [harness-generalization](harness-generalization.md) | 📋 | extends "pick one global winner" for `eval-matrix`/#29; pairs with the population-search row below. Guardrail: honour the fixed budget or a naïve ensemble multiplies eval cost |
| **Coordination-metrics as trace-level eval vocabulary** (overhead %, error-amplification Ae, redundancy R, message density, coordination-efficiency Ec — measured over real orchestrator→spoke runs) | [scaling-agent-systems](scaling-agent-systems.md) | 📋 | instrument `delegate_*` to log overhead + redundancy, then A/B "did fanning out actually pay off, or just burn tokens?" as an eval-gate metric — the trace-grading stance applied to *coordination*. Adopt the metrics, NOT the paper's fitted regression |
| **Capability-ceiling delegation heuristic** (don't fan out to a spoke when the orchestrator alone already handles the task — coordination has negative returns above a competence threshold, β=−0.408, overhead 58–515%) | [scaling-agent-systems](scaling-agent-systems.md) | 📋 | a delegation-decision signal for the orchestrator + an eval measure; complements difficulty-driven routing. External evidence for the lean-orchestrator/spoke-internal-tools rule (tool-coordination β=−0.330) |
| **Textual-gradient proposer for self-regen** (turn the eval failure into a *directed* NL "gradient" telling the proposer what to change — TextGrad) | [self-improving-survey](self-improving-agents-survey.md) + [aide2](aide2-recursive-self-improvement.md) | 📋 | a more sample-efficient proposer inside the UNCHANGED propose→verify→accept gate (safe by construction); experiment on `self_regen.py` |
| **Population/evolutionary search over overlays** (a population of candidates + selection > single-shot iterate) | [self-improving-survey](self-improving-agents-survey.md) + [aide2](aide2-recursive-self-improvement.md) + [kernel-forge](kernel-forge-mcts-optimization.md) | 📋 | natural extension once #29 P1 lands; `accept_change` already scores candidates — wire a small population loop on top. Kernel Forge adds the budget-aware MCTS variant: tree of partial edits with explicit spend ceiling, evidence it beats a single refine-chain wherever a fast objective evaluator exists |
| **Self-improvement taxonomy as a recurring self-audit** (re-map Prax onto the two-branch/8-cell taxonomy each time #29 moves) | [self-improving-survey](self-improving-agents-survey.md) | 📋 | cheapest gap-finder; Prax occupies every scaffolding cell — the least-developed is **memory *processing*/consolidation** (the next cell to look at) |

## Capability candidates

| Item | From | Status | Notes |
|---|---|---|---|
| **Persistent REPL session for `run_python`** (state, imports and variables carry across calls instead of a fresh `subprocess.run` each time) | [prime-agent](prime-agent.md) + [rlm](rlm-recursive-language-models.md) | 📋 | the difference between a REPL and a calculator, and the concrete first step of the code-as-action lane banked as a "narrow experiment". Must live in `prax-sandbox`, not the harness host |
| **Skills as importable packages promoted from the agent's own repeated work** (recurring workflows become code the agent can import next time) | [prime-agent](prime-agent.md) | 📋 | Prax has code-owned spokes and human-authored plugins; nothing the agent promotes itself. Route through the plugin trust tiers rather than free-form code |
| **WIRE `self_regen` — the loop exists and has zero consumers** (proposer, auditor, overlay application and `inoculate()` all present in `prax/eval/self_regen.py`; nothing calls them outside a CLI script, exactly like `accept_change`) | [prime-agent](prime-agent.md) | 📋 | **not a missing capability — a missing connection.** Prime Agent ships this as `/refine`; [Harness-R1](harness-r1.md) and [HarnessCompass](harness-compass.md) supply the invariants (frozen agent, read-only scorer, task-agnostic patches) that make wiring it safe |
| **Compute S_index over the eval matrix and retire saturated benchmarks** (S_index = exp(−R_norm²), R_norm = (s₁−s_k)/SE_Δ from published frontier leaderboards; publish it BESIDE each score and rotate anything in the high bins) | [benchmark-saturation](benchmark-saturation.md) | 📋 | **HumanEval is named as rapidly saturated** and our matrix carries several of that vintage. A saturated benchmark burns budget, pads the scorecard, and **makes real regressions invisible** — inside an accept-gate it is a gate that does not gate. Transfers to deterministic adapters only; `pass^k` and judge-scored goldens need tailored variance (their limitation #6) |
| **Shift eval investment from privacy toward expert curation** (resilience tracks curation, p=0.0017, NOT private test data — "no statistically meaningful difference"; privacy protects against CONTAMINATION, which is a different failure mode from SATURATION) | [benchmark-saturation](benchmark-saturation.md) | 📋 | corrects a half-assumption behind `prax-eval-battery`: it buys the contamination firewall (its actual stated purpose, still valid) but NOT discriminative longevity — a pristine secret set of easy items fails silently. Caveat: their private group is **N=4**, so act on the direction, not the strength. Reinforces the regeneration row over the secrecy one |
| **Spokes must enforce DISTINCT constraint families** ("which constraint does this spoke enforce that no other spoke does?" — two spokes that would accept/reject the same outputs are redundant as agents however different their prompts; personas partition evaluative authority, they are not ends in themselves) | [constraint-factorization](constraint-factorization-mas.md) | 📋 | an auditable gate for proposing a 16th spoke, and a lens for reviewing the existing 15. Complements the [scaling-agent-systems](scaling-agent-systems.md) capability-ceiling row: that says *don't fan out when the orchestrator suffices*, this says *when you do, make sure they disagree about something* |
| **Detect constraint infeasibility instead of oscillating** (jointly unsatisfiable constraints ⇒ no invariant solution exists; in practice this shows up as agents undoing each other and burning budget, not as an error) | [constraint-factorization](constraint-factorization-mas.md) | 📋 | live risk where **governance meets task**: a trifecta guard/approval gate unsatisfiable with the requested task should say "I cannot do this under these constraints" rather than loop. Same honesty principle as the calibration work |
| **Adaptive simplification** (scaffolding accretes and is never removed; when the default model tier changes, re-run the eval gate on the flags/ladders that tier justified and DELETE what no longer earns its keep — complexity is a safety surface, not just maintenance cost) | [harness-survey](agent-harness-engineering-survey.md) | 📋 | machinery already exists (flag A/B + `prereg.py` + the significance row); what's missing is the TRIGGER and the disposition to remove. The 2026-07-08 campaign's rejections of intent-clarification + deny-by-default were this, done once by accident |
| **Comprehension-preserving defaults in the shared workspace** (the degrading patterns are specific — auto-accepted edits + copy-paste prompting; make the UNDERSTANDING path cheap rather than the acceptance path expensive: readable diff + one-line why + trace one click away; keep real friction on risk tiers) | [impaired-programming](impaired-programming-comprehension.md) | 📋 | TeamWork-side UI work (cross-link its backlog per federate-by-ownership); Prax side already has the mechanisms (approval fingerprints, risk tiers, execution graphs, event log) — the gap is that watching is optional and auto-accept is frictionless. Second row: **preference ≠ outcome** — never read user satisfaction as evidence that oversight capacity was preserved; that needs its own signal |
| **Retained reasoning through the keyless proxy** (fix `llm_factory`'s `not _base_url` guard conflating third-party providers with the secrets proxy fronting real OpenAI: production keyless mode silently discards private reasoning after every tool call — the failure mode OpenAI measured at ~3× score / 6× output tokens on ARC-AGI-3 — and 404s `-pro`/o-series outright) | [openai-arc3](openai-arc3-harness-settings.md) | ✅ code / 📋 flip | **Shipped + verified live 2026-07-30**: `OPENAI_BASE_URL_IS_OPENAI` + `OPENAI_RETAIN_REASONING` (both default-off), o4-mini exercised through the live proxy on `/v1/responses` with `previous_response_id` chaining (ledger row). The proxy is path-agnostic by construction — no proxy change needed. Remaining: eval-gate A/B before recommending the flags; record harness/API settings in eval result records |
| **Lean toolchain in sandbox + governed `lean_check`** (+ axiom-audit trust gate) | [cdc-lean](cdc-lean-teach-prax-lean.md) | ✅ | Shipped + **verified live** on known theorems (`prax/agent/lean_tools.py`, `LEAN_TOOLS_ENABLED`; Lean 4.31.0 in the sandbox image, toolchain-only). The `sorry`/axiom trust gate works. |
| **Lean eval adapter** (miniF2F/PutnamBench; keyless seed + sandbox-scored halves) | [cdc-lean](cdc-lean-teach-prax-lean.md) | ⏸ | after `lean_check`; datasets stay in `PRAX_EVAL_DIR` |
| **Coding-agent benchmark adapter** (Terminal-Bench / SWE-bench-verified — real repo-level tasks, deterministic pass/fail on hidden tests, sandbox-scored) | [capy](capy-swe-agent-platform.md) | 📋 | closes the "marquee coding-agent bench" gap in the eval matrix (the sandbox-dependent option-3); Prax uniquely already owns the sandbox to run it; drops into the existing `prax/eval/benchmarks/` seam; un-spikeable by construction (hidden tests). Datasets in `PRAX_EVAL_DIR` |
| **Persona/user-simulator multi-turn evals, deterministic Pass@k** (τ-bench/τ²-bench) | [sierra](sierra-agent-platform.md) | ✅ framework | **Shipped** (`prax/eval/multiturn.py`, `make eval-multiturn`): persona-driven user-simulator ↔ agent loop, deterministic grading on the FINAL state (content on the last turn, tool/spoke on the union), **pass^k** (all k trials pass = reliability, not one lucky shot). Keyless-tested (injected stubs) + 3 seed cases. Live orchestrator+LLM-user-sim executors are unit-tested-only (VERIFICATION_LEDGER) — a live run is the next step |
| **"Executable world-models" — a general model-induction capability** (the **5-module verifier-centric stack**: perceive/abstract → propose executable hypotheses → execution-verify → counterexample-repair → plan-and-disambiguate; a `modeling`/`simulate` extension of the sandbox) | [arc-agi-3](arc-agi-3-schema-harness.md) + [exec-world-models](executable-world-models.md) | ⏸ precondition | **the feature, not a "game spoke"** — broadly useful (onboard unknown APIs, empirical debugging, simulation-planning, rule discovery). Prax already owns the load-bearing module (sandbox+codegen for propose/execute/repair); gaps are perception helpers + a disambiguating planner. Land with a **non-ARC** demo. Build after the shakedown runs clean |
| **Offline small-model ARC playbook** (program-synthesis+evolution > selective QLoRA-TTT > tiny-direct 2nd attempt > sampling; start from **Soar-qwen-7b** / NVARC Qwen3-4B / TRM, not a generic 14B) | [exec-world-models](executable-world-models.md) | ⏸ precondition | the model step-down ladder lands *here*. Recommended build: **NVARC-derived prediction + SOAR-style Python evolution + selective per-task QLoRA + TRM orthogonal 2nd attempt**. Honest bar: verified offline SOTA ~24% ARC-2 / ~8% ARC-3 — grand prizes are a moonshot for everyone; target a credible 20–30% open entry |
| **ARC-AGI-2 + ARC-AGI-3 benchmark adapters** (thin, on the capability above) | [arc-agi-3](arc-agi-3-schema-harness.md) | ⏸ precondition | adapters live in the eval engine (measurement, not special-casing). `arc_agi_2.py` first (static, deterministic exact-grid-match, keyless-CI-safe) then `arc_agi_3.py` (interactive, RHAE, `ARC_AGI3_ENABLED`, real games in `PRAX_EVAL_DIR`, mock for CI). ARC Prize 2026: $2M pool, offline Kaggle, OSS-required |
| **Self-authored ARC task generators** (seed from **`re-arc`** — Hodel's procedural generators for all 400 ARC-1 training tasks — not from scratch; + ARC-3 mini-games with *known* programs) | [arc-agi-3](arc-agi-3-schema-harness.md) + [exec-world-models](executable-world-models.md) | ⏸ precondition | unlimited, novel, **contamination-free** signal; trains the *general* capability not memorized answers. Public *train* half = dev, public *eval* half = held-out, never trained on. **generator + deterministic verifier = the un-gameable fitness function [#29](../IDEAS_BACKLOG.md) needs**; the proven template is **FunSearch/AlphaEvolve** (frozen LLM + deterministic evaluator + evolutionary population) |
| **Out-transparency the leaders: publish reasoning + induced schema programs** (not just action counts) as a public HF traces dataset | [arc-agi-3](arc-agi-3-schema-harness.md) | 📋 | sub-GB (frames as int-arrays); extends `trace.py`; public-set traces only (hidden eval uncontaminated); in-repo record stays aggregates-only |
| **Reproducibility-artifact bundling** (output + code + env + message history) | [claude-science](claude-science-workbench.md) | 📋 | extends trace records |
| **Traceability reviewer** (numbers/figures/citations trace to source) | [claude-science](claude-science-workbench.md) | 📋 | extends `claim_audit` |
| **World-model verify soundness** (verify induced programs against ground truth / held-out examples / an *independent* check — never self-report; "runs" ≠ "correct") | [lanyon](lanyon-formal-verification.md) + [exec-world-models](executable-world-models.md) | 📋 | design rule for the `prax/reasoning/worldmodel.py` loop — anti-*misformalization*; reproduces-every-example gate, counterexample survival, not "it didn't crash" |
| **Email as a channel** (OSS transport: Postal/Cloudflare) | [agentmail](agentmail-email-as-a-channel.md) | ⏸ | **ship only after** the lethal-trifecta guard — inbound email is a prime injection vector |
| **Mixture-of-Agents hard-task escalation** (`MOA_ENABLED`, default off) | [mixture-of-agents](mixture-of-agents.md) | ⏸ | gate rollout on the HAL `pass_per_1k_tokens` cost axis |

## Memory / learning (needs infra or eval coverage first)

| Item | From | Status | Notes |
|---|---|---|---|
| **ACT-R activation-based memory retrieval + async consolidation** (recency × frequency × association ranking; agent self-curates its store; NOOA ablates **+11.8 RHAE on ARC-AGI-3 from memory alone**) | [nooa](nooa-object-oriented-agents.md) | 📋 | flag-gated candidate ranking function for `memory_stm_*`/Qdrant recall — the memory-consolidation weakest cell finally has an evidence-backed design; gate on the memory eval battery, adopt the idea not their manager |
| **Learnable-vs-noise consolidation gate** (write experiences with *reducible/compressible* structure to long-term memory; skip noisy-TV traffic — flaky output, one-off chatter, irreducible randomness) | [learnable-novelty](learnable-novelty.md) | 📋 | the missing "is this worth remembering?" criterion for the weakest scaffolding cell (memory *consolidation*, per [self-improving-survey](self-improving-agents-survey.md)). Heuristic (surprising **and** recurring/compressible), NOT the literal reservoir estimator (frozen observer saturates). Pairs with the sleep-phase row below |
| **Unlearning / memory-obsolescence** (delete/decay stale or wrong long-term memories — the *forget* side of consolidation; Prax's two-layer store has no deletion story and stale facts accrete) | [coala](coala-cognitive-architectures.md) | 📋 | CoALA names it an open problem; pairs with the consolidation-gate above + the sleep-phase prune below. Gate on the same held-out retrieval-probe metric |
| **Memory-writes as first-class GOVERNED actions** (a write to Qdrant/Neo4j — esp. one derived from untrusted tool output — gets the same governed-tool audit/risk-tier as an external action) | [coala](coala-cognitive-architectures.md) | 📋 | CoALA puts *learning* in the same action space as tool calls; connects to the provenance-tainting middleware (untrusted output shouldn't silently become durable memory) |
| **Git-tracked memory** (the workspace is ALREADY a git repo and the Library lives in it — nothing reads that history: no diffing a fact against its previous value, no "when did I start believing this?") | [memgpt/letta](memgpt-virtual-context.md) | 📋 | Letta moved from opaque memory blocks to git-versioned memory (MemFS). Prax has the substrate and does not use it as memory. Cheap to reach — the commits already exist |
| **Working memory ranked by importance, not recency** (`importance` was written on every STM entry and never read; selection was `stm_entries[-5:]`, so a 0.9 fact fell out behind five pieces of trivia with nothing said) | [memgpt](memgpt-virtual-context.md) | ✅ | Shipped: importance-ranked with recency tie-break, truncation announced + names the tool to get the rest, hidden-count shown. **Correction:** my first note claimed STM was never injected at all — it always was, via `build_memory_context`; I had grepped one file and generalised |
| **Agent-initiated context compaction** (two governed tools: `context_compact` — LLM-summarise + offload raw turns to storage under an id, demotion never deletion; `context_recall` — retrieve by id. The AGENT decides when, from its own reasoning state) | [memgpt](memgpt-virtual-context.md) → [acm](acm-agentic-context-management.md) + [nooa](nooa-object-oriented-agents.md) + [openai-arc3](openai-arc3-harness-settings.md) | 📋 | ACM corrects the earlier row: build a capability, not a fullness warning. Two independent later sightings strengthen it: NOOA's context blocks as model-callable API (regions as first-class objects) and OpenAI's production compaction numbers (3× score / 6× fewer tokens vs rolling truncation). First customer for `prereg.py` — kill condition written before any run: flag dies if compaction doesn't cut peak tokens on long-horizon goldens at unchanged private pass-rate. Expect NOTHING on the cheap tier (their "weak models collapse first" caveat); apparent cheap-tier gains mean audit the measurement. Deletion stays governed. CRUX constraint: **compaction must preserve standing instructions** (their agents' multi-day instruction drift was explicitly "context rot during compaction" — pin goal/rule blocks through every compaction) |
| **Scheduled "sleep phase"** (offline replay → re-distill/prune memories) | [lm-sleep](lm-sleep-consolidation.md) | 💤 | task_runner-shaped; **gated on held-out retrieval-probe evals** (don't build the maximizer before the un-gameable metric) |
| **SEAL → Sleep recipe** for the local-model finetune lane | [lm-sleep](lm-sleep-consolidation.md) + [expert-judgment](expert-judgment-finetune.md) | 💤 | GPU-gated; the recipe to reach for when the finetune lane opens |
| **Learning-curve harness-lift + experience-reuse ablation** | [edge-bench](edge-bench-learning-curves.md) | 📋 | an eval *method*, not the gated task set |
| **Non-stationary / no-reset "does memory actually adapt?" eval** (adaptation/recovery/forgetting, curve-not-point) | [morpheus](skyfall-morpheus-continual-learning.md) | 🔨 | **Banked as a tracked golden** (`memory_adaptation_under_drift.yaml` — single-turn, measures the adaptation *reasoning*); the full multi-config curve-not-point *harness* is still 📋 (same bucket as learning-curves + τ-bench; build the metric before any memory maximizer) |
| **Trace-grading** (score the process — committed/verification/efficiency — not just the answer) | [verify-and-commit](verify-and-commit-discipline.md) | ✅ | `prax/eval/trace_grade.py` + `docs/guides/trace-grading.md`; praxbench Q1 is the first dual-axis consumer |
| **Verify-discipline hint** (`VERIFY_DISCIPLINE_ENABLED`, default off) | [verify-and-commit](verify-and-commit-discipline.md) | 🔨 | `_VERIFY_DISCIPLINE_HINT` in orchestrator; **eval-gate + trace-grade before any flip** (prompt hints have underperformed) |
| **Per-call output cap + forced commitment** (fixes GPQA single-call non-commitment) | [verify-and-commit](verify-and-commit-discipline.md) | 📋 | structural — a between-steps hint can't reach a single runaway call; **truncation risk, needs eval validation** |
| **Verify-once memoization — idempotent reads** (`TOOL_MEMOIZE_ENABLED`, default off) | [verify-and-commit](verify-and-commit-discipline.md) | 🔨 | `IdempotentToolCache` in `loop_middleware.py`; per-turn cache, reads-only (run_python/shell/writes/browser NEVER). Grade with trace-grade before flip |
| **Effectful over-verification** (run_python ×6) — still open | [verify-and-commit](verify-and-commit-discipline.md) | 📋 | can't be safely memoized (side effects); needs purity metadata or prompt-level consolidation — the A/B's actual finding, deliberately NOT force-fixed |
| **Code-REPL orchestration experiment** (CodeAct: tools/spokes as REPL functions, big intermediates in variables) | [rlm-harness-lid](rlm-harness-lid.md) | 📋 | the LID "context offloading" lever, newly plausible via `run_python`. **Narrow, instrumented experiment on ONE spoke — NOT an orchestrator rewrite** (L2/L3 fragile per the autonomy taxonomy); measure long-horizon lift via trace-grade + capability suite before committing |
| **Keep large task-specific data OUT of the root context** (LID principle) | [rlm-harness-lid](rlm-harness-lid.md) | ✅-partial | already half-true: spoke-summarization + prompt-selectivity + context budgets. The cheap standing takeaway; audit for tool results that still flood the orchestrator |
| **Trajectory monitor: pause + alert + resume** (promote spiral/claim-audit/trifecta from flag-log to *pause the session + escalate to human + resume-on-approval*) | [openai-long-horizon-safety](openai-long-horizon-safety.md) | 📋 | **highest-value safety adopt** — per-action guards fail on long rollouts; watch the *outcome the sequence heads toward*. Prax detects but doesn't cleanly pause+hand-to-human |
| **Systematic incident-derived evals** (observed failure → praxbench/golden at real horizon) | [openai-long-horizon-safety](openai-long-horizon-safety.md) | 📋 | make the ad-hoc "turn a failure into an eval" a discipline; matches never-spike/audit-the-check |
| **Late-rollout constraint re-assertion** (models forget the constraint late in long turns) | [openai-long-horizon-safety](openai-long-horizon-safety.md) | 📋 | fold into verify-and-commit; re-assert load-bearing constraints late in long turns |
| **Secrets proxy — KEYLESS Prax (Tier 1)** (proxy holds keys, injects into OpenAI+Anthropic calls, streams back) | [secrets-proxy](../security/secrets-proxy.md) | ✅ | **built as a separate, isolated repo** ([`praxagent/prax-secrets-proxy`](https://github.com/praxagent/prax-secrets-proxy)) — real isolation is process/filesystem separation, not a second env file Prax can `open()`; opt-in (default Prax unchanged). Prax runs with placeholder keys, nothing to steal (the "make the secret unreachable" wall). Allowlist + audit log; 10 keyless tests + live smoke. Prax-side hook: `ANTHROPIC_BASE_URL` + `OPENAI_BASE_URL` |
| **Secrets proxy — Tier 2 (general egress + allowlist)** (Twilio/search/ElevenLabs; transparent forward proxy) | [secrets-proxy](../security/secrets-proxy.md) | 📋 | extend to the ~5 other key sites; the transparent-proxy variant also delivers the egress allowlist that kills the data-exfil leg |
| **Silent-failure test suite for `prax-secrets-proxy`** (stateless-per-request under concurrency; stream-integrity; auth isolation) | [failure-atlas](failure-atlas-llm-gateways.md) | ✅ | **shipped** — 4 invariant tests in the proxy repo (interleaved + 20-concurrent no credential-bleed; per-call body isolation; truncated-upstream surfaced not silently 200-ed). Pins the trust-critical *stateless pass-through* design |
| **Record the model that *actually* answered** (not just the requested one) on the failover path | [failure-atlas](failure-atlas-llm-gateways.md) | ✅ | **shipped** (PR #144) — `LLM_RECORD_ANSWERING_MODEL` (default off): warns + telemetry on silent model substitution; `_record_answering_model` in `orchestrator.py`, prefix/date-suffix tolerant |
| **Bound failover retries** (cap + backoff + jitter; 429 fails *over*, never blocks) | [failure-atlas](failure-atlas-llm-gateways.md) | ✅ | **shipped** (PR #144) — retries already bounded once-per-provider (`_tried_providers`); added `LLM_FAILOVER_BACKOFF_MS` (default 0) jittered backoff on the failover retry (`_failover_backoff`) |
| **"Loud-vs-Silent" as a standing design-review lens** (ask "what silent-200 corruption could this introduce?") | [failure-atlas](failure-atlas-llm-gateways.md) | 📋 | apply to any future middle-tier component (caching, inspector, Tier-2 egress); the transferable idea, not code |
| **Sandbox key-in-env exposure** — REMEDIATED | [sandbox-execution-boundary](../security/sandbox-execution-boundary.md) | ✅ | model keys removed from the sandbox image by default (prax-sandbox #4) + coding-session tools removed entirely (#142); keyless container is the default now |
| **Anthropic prompt caching** (`cache_control` on the system prompt) | [opencode-critique](opencode-critique-eval.md) | 📋 | real gap — Claude path caches nothing today. Seam `orchestrator.py:1191`; apply AFTER `prepare_context` (list-vs-string), **verify live cache hits** before flip |
| **Measure cache-hit rate + re-weigh `PROMPT_SELECTIVITY` cross-turn** | [opencode-critique](opencode-critique-eval.md) | 📋 | selectivity (recommended-on) likely defeats cross-turn OpenAI caching; the flag campaign measured per-call tokens, NOT cache reuse — instrument, then A/B on *total* cost |
| **System-prompt ordering: stable-first, volatile-last** (extend cached prefix) | [opencode-critique](opencode-critique-eval.md) | 📋 | hints sit after volatile temporal/memory; reorder is behavior-adjacent → flag+eval |
| **Train/serve weight-sharing is a ceiling on the finetune pipeline** — name it, don't patch it | [tinker](tinker-training-api.md) | 📋 | `finetune_service` (unsloth subprocess) and vLLM adapter-load hand off a *directory*; adequate for SFT-on-corrections, **structurally incapable of on-policy RL**. Recorded so "add RL" is scoped as a seam rebuild, not a feature |
| **GPU executor goes in `prax-sandbox` behind the existing `JobSpec` target — NOT a new `prax-infra` repo, NOT in prax-lab** | [tinker](tinker-training-api.md) | ⏸ | architectural decision, recorded so it isn't re-litigated. Precondition: actual hardware (or a rented trainer). `exec_api.py:141` already refuses `gpu > 0` honestly; the contract is done, the executor isn't |
| **Search over `.progress/` detail files** (they are date-addressed only today) | [prolong](prolong-programmatic-memory.md) | 📋 | **concrete shipped gap**: `progress_detail(slug, date)` needs a date the agent usually lacks; nothing under `prax/agent/` greps `.progress/`. The complete record is preserved and then made unretrievable. Fix = a search tool, NOT more compaction |
| **Standing rule: compact the CONTEXT, never the RECORD** | [prolong](prolong-programmatic-memory.md) | 📋 | sharpens the [ACM](acm-agentic-context-management.md) compaction row rather than reversing it — compaction is right when the model reads its own context, unnecessary when code greps a file. Apply as a design-review lens to any future memory/compaction work |
| **Programmatic append-only log for long-horizon turns** (harness writes, agent greps) | [prolong](prolong-programmatic-memory.md) | 📋 | Prax has the workspace + `run_python` + sandbox; missing the harness half. **Needs no persistent REPL** — unblocks the long-horizon lane the [Prime Agent](prime-agent.md) REPL gap appeared to block. Flag-gated + eval-gated; a candidate, not a decision |
| **Four-condition memory ablation** (full / windowed / none / stateless workspace) | [prolong](prolong-programmatic-memory.md) | 📋 | we have per-flag A/Bs but have never asked whether the complete record beats a window, or beats nothing, across Qdrant + Neo4j + `progress_*` + `memory_stm_*` — the cell repeatedly named our weakest |
| **Symbolic consistency pass at memory consolidation** (contradiction detection as a graph query, not an LLM opinion) | [neurosymbolic-lens](neurosymbolic-lens.md) | ✅ | SHIPPED 2026-08-06: `memory/consistency.py` + `graph_store.current_targets()` + hook in consolidation, behind `MEMORY_CONSISTENCY_ENABLED` (log-only) / `MEMORY_CONSISTENCY_AUTO_SUPERSEDE` (acts). Allowlist deliberately excludes the extraction prompt's default (all multi-valued) types — a test pins that. Detection symbolic, resolution neural |
| **Standing rule: neural proposes, symbolic disposes — build symbols on the CHECKER side** | [neurosymbolic-lens](neurosymbolic-lens.md) | 📋 | generators compete with the model and lose; checkers gate it and never get worse. Design-review lens: at every neural→downstream junction ask "is there a symbolic check here, and is its ground truth independent of the proposer?" |
| **`EMBEDDING_BASE_URL` — embeddings routable to any OpenAI-compatible local server** | [sie](sie-unified-local-inference.md) | ✅ | SHIPPED 2026-08-06: the `openai` embedding path had no explicit base URL (chat was locally routable, embeddings silently weren't); keyless-local placeholder key; unset = prior behaviour. Guide section in `local-cpu-inference.md`; SIE itself unverified here (no GPU) |
| **Abstractions must keep a deterministic pointer to their evidence** | [tencentdb-agent-memory](tencentdb-agent-memory.md) | ✅ | SHIPPED 2026-08-07: `_compact()` destroyed the `short_id`s when folding bullets into the Archive paragraph, orphaning detail files exactly when the summary became all that's in context. Compaction now appends a bounded `Sessions: ...` trailer and `progress_detail` dereferences `YYYY-MM-DD-{id}`. Sharpens the [PRO-LONG](prolong-programmatic-memory.md) rule: **dereference beats search** |
| **Vector/semantic conflict detection at consolidation** (complement to the shipped symbolic check) | [tencentdb-agent-memory](tencentdb-agent-memory.md) | ⏸ | theirs is on by DEFAULT and catches semantic conflicts (dark-vs-light mode) our type-level check structurally cannot; ours is deterministic where theirs needs a threshold. Sequenced AFTER the symbolic pass's log-only shakedown, not before |
| **Mermaid canvas as a context-offloading encoding** | [tencentdb-agent-memory](tencentdb-agent-memory.md) | 📋 | third context-offloading sighting and the first with a concrete encoding (state graph in context, `node_id` drill-down on error, full logs on disk). Substantial feature with a real eval question — candidate, not a decision |
| **Injection checks measure the ACTION, not the mention** | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | ✅ | SHIPPED 2026-08-07: new `regex_absent` + `tool_absent` kinds; `absent:` on an attacker address failed transparent refusal while PASSING silent compliance. Fourth time "audit the checker first" paid out |
| **Harness scaffolding excluded from graded artifacts** | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | ✅ | SHIPPED 2026-08-07: `instructions.md` (~67KB) was folded into the graded answer, able to satisfy checks the agent never earned and crowding out real artifacts at an 8000-char budget |
| **Scorer freeze enforced by the A/B runner** | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | ✅ | SHIPPED 2026-08-07: `scripts/flag_ab.py` fingerprints `capability.py` + case YAMLs and ABORTS on mid-campaign change. I broke this rule the same night — enforcement now sits in the tool, not in my memory |
| **Grow the capability suite past ~30 cases, or stop using pass-rate for flag decisions** | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | 📋 | n=7 gives a 95% CI of 35.9–91.8%; any arm at 4/7 or 6/7 is indistinguishable from baseline. Until then: lead verdicts with token deltas, treat pass-rate as a regression tripwire, and publish the CI beside every number |
| **Never compare arms on `avg_tokens`; use per-case deltas** | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | 📋 | one case is 42% of the suite's tokens, so an unweighted mean measures that case's variance and calls it a flag effect. Cost July's campaign a wrong −7% for middleware and a 5×-understated −2% for selectivity |
| **Every campaign needs a NULL ARM** (a flag that provably cannot affect the metric) | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | ✅ | SHIPPED 2026-08-07 as the `baseline_replicate` arm in `flag_ab.py`. `MEMORY_CONSISTENCY_MODE=log` served as an accidental null arm and gave the 0.45% floor that made every other verdict interpretable |
| **`PROMPT_SELECTIVITY_ENABLED` saves ~10.5% tokens (~25× noise floor)** — measured | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | ✅ | default flipped ON in code 2026-08-07 and now confirmed on far stronger evidence than the flip was made with |
| **`AGENT_MIDDLEWARE_ENABLED` has NO cost benefit** (costs ~0.5%) | [eval-scorer-audit](eval-scorer-audit-2026-08-07.md) | ✅ | July's −7% was an averaging artifact. Flip stands on no-regression + injection defence-in-depth only; `.env-example` corrected to stop citing a saving that does not exist |
| **ADVISOR — a second model, own context, watching every turn inline** | [omp](omp-coding-agent.md) | 📋 | the structural version of `claim_audit`, which is post-hoc and advisory (runs after the reply exists, changes nothing by default). Theirs runs DURING, on an independent model — maker≠checker applied live rather than at grading time |
| **Time-traveling stream rules — abort mid-token, inject, retry from the same point** | [omp](omp-coding-agent.md) | 📋 | EVERY Prax guard is post-hoc. This is a candidate answer to stochastic injection resistance: intervene at generation time rather than grade afterwards. Needs a trip budget + flag; aborting/resuming is provider-dependent |

## The standing structural gaps (tracked in depth elsewhere — pointer, not a re-list)

The [2026 agentic-landscape sweep](agentic-landscape-2026-sweep.md) found the field's only genuine leads over Prax are three, and they remain the highest-priority *structural* work:

1. **Indirect prompt-injection / lethal-trifecta defense + injection evals** — partially shipped (lethal-trifecta guard); the **injection eval coverage** (AgentDojo-style) is still the #1 gap and **gates** the email channel above. Also the OpenClaw security taxonomy self-audit ([nemoclaw](nemoclaw-openclaw.md), 10 vuln classes).
2. **Durable checkpoint-resume** — `CHECKPOINT_BACKEND`/`CHECKPOINT_RESUME_ENABLED` exist (unit-tested); the adopt step is the eval-gated flip.
3. **In-context memory management** (compaction / tool-result clearing) — ongoing.

See that doc's 16-item prioritized punch-list for the full accounting; this tracker deliberately does not duplicate it.

---

**Maintenance rule.** When a research assessment lands an adopt-candidate, add a row here in the same PR (the assessment argues *why*; this row tracks *whether/when*). When an item ships, flip it ✅ and point at the code. A parked (⏸) item names the decision or precondition it waits on, so "parked" never quietly means "forgotten."
| **PROMPT CACHING — Prax emits no `cache_control` / ephemeral blocks / caching header at all**; ~97% of input tokens are re-sent conversation prefix, re-paid every tool-calling round | [crush](crush-charm-coding-agent.md) | 📋 | task #59. MEASURE FIRST — OpenAI-compatible endpoints may already cache server-side; Anthropic needs explicit breakpoints Prax never emits; confirm the secrets-proxy doesn't strip them. Win is COST, not correctness |
| **Adaptive per-tier scaffolding profiles learned from `harness_lift`** — does the lift's SIGN flip between a small and a large model? | [crush](crush-charm-coding-agent.md) | 📋 | task #60. Prax owns the only instrument that measures this. Smallest honest experiment first: if no component flips sign, the idea dies cheaply. Predicted independently by adaptive-simplification ([agent-harness-survey](agent-harness-survey.md)) and Weng's middle-tier finding → pre-register it |
| **LSP as a context source for the coding lane** (language servers in `prax-sandbox`, behind a governed tool) | [crush](crush-charm-coding-agent.md) | 📋 | "verifiable beats judgeable" applied to code navigation — a language server cannot hallucinate a definition, grep can. Same shape as the shipped `lean_check` toolchain pattern. 2nd sighting overall ([omp](omp-coding-agent.md) declined it as coding-vertical; the Niklaus result argues it matters more than assumed) |
| **Agent Skills as packages (`SKILL.md`)** | [crush](crush-charm-coding-agent.md) | 📋 | **2nd sighting** — [Prime Agent](prime-agent.md) named it first, so it's no longer one project's opinion. Plugins are the right shape for credentialed capability, too heavy for procedural knowledge. Design constraint: a skill is untrusted content that shapes behaviour → must arrive tainted and must NOT widen a capability ceiling |
| **Local model endpoint auto-discovery** (Ollama / llama.cpp / LM Studio / litellm) | [crush](crush-charm-coding-agent.md) | 📋 | cheapest item here, no architectural consequence; removes the manual `OPENAI_BASE_URL` friction the [SIE](sie-unified-local-inference.md) assessment was about |
| **Context offloading** — a large tool result goes to a file/variable; the model sees constant-size metadata + a pointer, never the payload | [rlm](rlm-harness-lid.md) (2026-08-08 paper update) | 📋 | PROMOTED from banked. Best-evidenced cheap scaffold change we have seen: +50pp on CodeQA for Claude Code (12.0→62.0) and 0.0→84.0 on BrowseComp+, no recursion and no training. Same shape [PRO-LONG](prolong-programmatic-memory.md) argued from the memory side. Directly relevant to the 1M-token runaway measured in spiral-20260808 |
| **Persistent REPL** (hold an object symbolically across calls) | [rlm](rlm-harness-lid.md) + [prime-agent](prime-agent.md) | 📋 | **3rd sighting.** `run_python` base64s a script through `docker exec` — fresh process per call, no variable survives. Blocks any RLM-shaped work. RLM depth=0 (REPL, NO sub-calls) already beats most baselines, so the environment is the load-bearing part, not the recursion |
