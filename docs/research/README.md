# Research — Academic Foundations for Agentic Workflow Design

This section summarizes empirically validated findings from academic research and production systems that inform Prax's architecture. These aren't theoretical — each finding has been demonstrated to improve agent performance on real benchmarks.

## Contents

- [Planning & Reflexion](planning-reflexion.md) — §1-§2: Explicit planning, self-verification
- [Orchestration](orchestration.md) — §3-§5: Bounded sub-agents, workspace persistence, context management
- [Grounding](grounding.md) — §6-§7: Error recovery via checkpointing, anti-hallucination
- [Production Patterns](production-patterns.md) — §8-§10: Framework patterns, tool overload, content pipelines
- [Pipeline Composition](pipeline-composition.md) — Hardcoded vs dynamic pipeline synthesis; skill libraries; LLM-Modulo
- [Plugin Sandboxing](plugin-sandboxing.md) — §11: The Glass Sandbox problem and process isolation
- [Model Routing](model-routing.md) — §12-§13: Thompson Sampling, difficulty-driven routing
- [Error & Metacognition](error-metacognition.md) — §14-§15: Multi-perspective recovery, failure profiles
- [Active Inference](active-inference.md) — §16-§18: Self-verification, Active Inference theory, empirical validation
- [Benchmarking](benchmarking.md) — §19: Why external validation matters
- [Eval Status — 2026-04-09](eval-status-2026-04-09.md) — Current state of the GAIA eval cycle: 4 tasks, 14 runs, 38% pass rate, 4 systemic fixes shipped, what to pick up next
- [Prax Benchmarks — Agentic Harness Evaluation Plan](prax-benchmarks.md) — The practical "what to run, in what order" catalog covering 25+ benchmarks across capability, safety/adversarial, and methodology dimensions: harness architecture (environment/scoring/control/reporting), comparative coverage tables, GAIA, τ-bench, AgentDojo, SWE-bench Verified, Terminal-Bench, BrowseComp, WebArena, TheAgentCompany, BFCL, ToolBench, SafeArena, AgentHarm, HCAST time-horizon framing, methodological pitfalls, and the HAL/Inspect-AI harness tooling recommendation
- [awesome-evals (BenchFlow) — curated eval bibliography](awesome-evals.md) — Reference note on the [benchflow-ai/awesome-evals](https://github.com/benchflow-ai/awesome-evals) annotated bibliography (386+ links across eval methodology, infra, judge design, integrity, RL environments, agent/safety eval). **Verdict: document + adopt the *methodology*, not the benchmarks** — Prax already owns the benchmark catalog (`prax-benchmarks.md`). Net-new for Prax's *existing* golden infra: (1) **"verifiable beats judgeable"** (only 2 of 8 goldens are comparator-scored — lean toward deterministic checks); (2) **binary per-criterion judging > the current 0–1 Likert** in `goldens.score_golden`; (3) judge calibration / dedicated evaluator models (Prometheus 2, Atla Selene); (4) integrity/holdout/label-error hygiene; (5) RewardBench + verifiers for the eventual RL fine-tuning (#14 Phase 2 / cold-start). Single load-bearing idea: push evals *and* RL rewards toward deterministic checks wherever the task permits.
- [Judge robustness on fuzzy tasks (Diffuse AI Control)](diffuse-ai-control-judge-robustness.md) — Reference note on *"Diffuse AI Control on Fuzzy Tasks"* ([arXiv 2606.08892](https://arxiv.org/abs/2606.08892) + Anthropic Alignment companion). **Read for the eval-robustness method, not the AI-control threat model** (an untrusted model deliberately sandbagging isn't Prax's situation). The transferable finding: **weak LLM judges on fuzzy/subjective tasks are systematically gameable by impressive-but-vacuous outputs** (the "totalizing" attack — sweeping claims in borrowed jargon), and a hardened scoring prompt resists it. Directly Prax's case — its golden judge is a LOW-tier (weak) model on irreducibly-judgeable criteria. **Shipped:** (1) `_SCORE_PROMPT` hardened to grade *substance over polish* (score 0 for confident vacuity; reward task-specific specificity; "if you can't point to the span that satisfies it, it isn't satisfied"); (2) a high-tier **supervising auditor** (`score_golden(audit=…)`, `EVAL_AUDITOR_ENABLED`, #26) that re-checks the cheap judge's *passes* and can veto them — maker≠checker on the judge. The complement to `verify` (deterministic where mechanical) + #22 (independent accept signal). Prax takes the static hardening + the stronger-proxy auditor, but **not** the paper's red/blue prompt-*optimization* (needs a ground-truth scorer Prax lacks for fuzzy goldens).
- [Sierra's agent platform — what Prax can learn](sierra-agent-platform.md) — Reference note on [Sierra](https://sierra.ai) (Bret Taylor/Clay Bavor) via a [Zack Reneau-Wedeen interview](https://youtu.be/uCKhOmth2ms): a production "agent OS" for customer-facing agents (chat/voice/commerce). **Verdict: document + adopt the *evaluation methodology*; reinforce directions Prax is already converging on; document-don't-adopt voice/PCI-payments/commerce until those are product directions.** Load-bearing transferable: **persona/user-simulator multi-turn evals graded deterministically on state with Pass@k** — Sierra open-sourced exactly this as [τ-bench](https://github.com/sierra-research/tau-bench)/[τ²-bench](https://github.com/sierra-research/tau2-bench), the biggest gap vs Prax's single-turn capability suite and CPU-cheap because grading is deterministic. Also: memory-as-primitive, "context engineering = everything it needs nothing more," simulation-as-release-gate (which the new `make eval-capability`/`eval-harness-lift` suites already embody). Cross-links [prax-benchmarks.md](prax-benchmarks.md).
- [AutoResearch / labless.dev — the self-improvement loop](autoresearch-labless.md) — Reference note on [labless.dev](https://labless.dev/) ("live collaborative autoresearch for nano projects") and the pattern it productizes, [Karpathy's AutoResearch](https://github.com/karpathy/autoresearch): an agent hill-climbs a single-GPU nanochat run overnight — edit→fixed 5-min experiment→measure one metric→keep/discard, driven by a `program.md`. **Verdict: document + adopt the *loop pattern* (it's the concrete mechanism for self-regeneration #29), not the product or the nanochat target.** Load-bearing insight: an autoresearch loop is a benchmark *maximizer*, so its dominant failure is reward-hacking the eval — and Prax has spent this arc building the **safety preconditions** (deterministic/`verify` grading, maker≠checker auditor, "abstraction-not-example", cost-per-accepted-change) that make closing the loop safe. The new capability/harness-lift/GAIA suites are the fitness function; the resumable CPU/overnight runner is the execution substrate; TeamWork is the collaborative watch/steer surface. Ties the self-regeneration direction (#29) to the world-class-evals work.
- [matrix.build — autonomous multi-agent "company" swarm](matrix-autonomous-company.md) — Reference note on [matrix.build](https://matrix.build/) ("launch a 0-Person Company that actually earns") — a swarm of Neo/Claude-Code/Codex agents (each with own browser/tools/files/memory) in department-coordinated continuous loops to build/ship/distribute/earn. **Verdict: document-don't-adopt**, with an epistemic caveat (signal is a marketing page; "actually earns" is unverified — treat as vision, not evidence, per the Fugu-cite precedent). The substrate (per-agent isolation, coding-agent orchestration, continuous loops, proof-of-work) is already Prax's via sandbox + task-runner + observability; the one novel idea — a multi-agent **org/department** layer — is a *deliberate non-goal* vs Prax's bounded single-orchestrator+spokes (the ~50-tool reliability bound). The genuinely relevant adjacent is **agent-matrix** (open "PyPI/Docker Hub for agents/MCP" registry) — an interoperability adopt-candidate to track alongside ARD/OKF, not the company-builder.
- [Agentic landscape sweep (2026) — adopt/track/skip vs Prax](agentic-landscape-2026-sweep.md) — Fan-out survey of **67 platforms** across 9 categories + the 5 single assessments, ranked for action. **The field's genuine lead is only 3 things**: (1) indirect prompt-injection / lethal-trifecta defense (Prax has zero injection evals or taint-tracking — the #1 structural gap), (2) durable checkpoint-resume, (3) in-context memory management (compaction/notes/tool-result-clearing). Everything else is hype-to-skip or **independent convergence ONTO Prax's bets** (bounded single-orchestrator validated by Cognition/CrewAI/ADK; the un-gameable verifier validated by DGM/AlphaEvolve/pipeline-math; MCP+A2A interop). 16-item prioritized ADOPT punch-list, all low/medium-effort reusing infra Prax owns (maker≠checker auditor, local VLLM, deterministic grading, glass-sandbox, capability gateway). Includes [pipeline-math](https://github.com/Pengbinghui/pipeline-math) as the worked example of the un-gameable-verifier (Lean-formal-proof) pattern = the engine behind "breakthrough" results.
- [Mixture of Agents (Nous Hermes) — should Prax have it?](mixture-of-agents.md)
- [AgentMail — can Prax replicate it with OSS?](agentmail-email-as-a-channel.md) — AgentMail gives AI agents their own email inbox/address (send/receive + threading, webhooks, SDKs, MCP, IMAP/SMTP, DKIM/SPF/DMARC). **Verdict: YES, the core is OSS-replicable — email is just another Prax channel.** The agent layer is already native (multi-channel + `source` tracking + deterministic identity + governed send tool + existing MCP server + conversation store); only the TRANSPORT layer is real infra — pluggable to OSS **Postal** (closest 1:1) / Cloudflare Email Routing, or a managed relay for deliverability (the one genuinely hard part). **Load-bearing catch:** inbound email = prime injection vector, outbound = external sink → the email channel MUST route through `LETHAL_TRIFECTA_GUARD` + is exactly what the AgentDojo injection goldens grade. Adopt-candidate: build the channel, buy/self-host the transport; ship after the trifecta guard. — MoA = **ensemble-for-quality** (reference models analyze first → an **aggregator** model writes the answer + emits tools); HermesBench ~6pts over the best component, at the cost of more model calls/turn. Explicitly **not a swarm** → compatible with Prax's bounded orchestrator; a cousin of the shipped **maker≠checker** + cross-provider + tiers. **Verdict: adopt flag-gated (`MOA_ENABLED`, default off) as a hard-task escalation, NOT default — and gate rollout on the new HAL cost-axis** (`pass_per_1k_tokens`): keep MoA only where the reference-ensemble lift beats simply upgrading the tier at equal spend.
- [Replicating expert judgment (Thinking Machines) — why Prax can't do this yet](expert-judgment-finetune.md) — Thinking Machines fine-tuned **Qwen3-235B** to **84.7%** on financial document-triage (vs ~78% for frontier GPT-5.5/Opus-4.8/Gemini-3.1), **13.8× cheaper** — "custom models on expert-labeled data beat frontier prompting" because prompting can't articulate *tacit* judgment. Recipe: interleaved batching + CISPO loss + on-policy distillation w/ new-high teacher promotion (GRPO 73.5%→84.7%); the crux was **data curation** (non-expert labels FAILED → train, find model/label disagreements, expert-relabel those). **Why Prax can't yet:** the fine-tune spoke is MVP LoRA-SFT (no CISPO/OPD recipe), no disagreement-curation loop, CPU/cheap ≠ 235B GPU — but the substrate is there (Qwen3 backend, evals, feedback), and it's the self-regen loop applied to WEIGHTS not scaffold (tracked as SEAL). **Adopt now:** the **disagreement-driven data-curation loop** — use maker≠checker as the disagreement detector to clean eval-golden labels (data quality > model size).
- [Claude Science (Anthropic) — is Prax capable of this?](claude-science-workbench.md) — Anthropic's beta life-sciences workbench (2026-06-30): a generalist coordinating agent + 60+ curated skills/connectors, an **actor-critic reviewer** (flags bad citations / untraceable numbers / figure-code mismatch, self-corrects), **reproducibility artifacts** (output + exact code + environment + full message history), and **compute mgmt** (draft plan → approve → run on YOUR laptop/Linux/HPC). Connectors: UniProt/PDB/Ensembl/ClinVar/ChEMBL/GEO + NVIDIA BioNeMo (Evo2/Boltz2/OpenFold3). **Its architecture near-mirrors Prax** (coordinating agent+spokes, maker≠checker reviewer, sandbox-on-user-infra, reproducible traces) — strongest validation yet, from Prax's own model provider. **Is Prax capable? Architecturally YES, domain content NO** — the science skills/DB-connectors/BioNeMo models are plug-and-play (MCP + VLLM/GPU rails), not a re-architecture. **Adopt-now (domain-agnostic):** (1) first-class reproducibility-artifact bundling; (2) a **traceability reviewer** (numbers/figures/citations trace to source) extending claim_audit. Document-don't-adopt the product/vertical.
- [NeMoClaw / OpenClaw (NVIDIA) — lessons for Prax](nemoclaw-openclaw.md) — The closest external peer to Prax: **OpenClaw** ("originally a personal AI assistant, grew into a general agent platform" — Prax's own origin story), and **NeMoClaw**, NVIDIA's secure enterprise distribution wrapping it with OpenShell sandboxes + local Nemotron + MCP + lifecycle/governance. **Verdict: adopt the *security checklist*, treat the architecture as strong validation, borrow two patterns, don't replace Prax's stack.** Lesson 1 (strongest): the [OpenClaw security taxonomy (arXiv 2603.27517)](https://arxiv.org/pdf/2603.27517) gives 10 vuln classes (tools/memory/sandbox/MCP) to self-audit against — Prax is STRONG on MCP-auth/command-injection/resource-limits but has GAPs on **indirect prompt injection via tool outputs** (#1 prevalent), memory poisoning, confused-deputy, tool-combination escalation. Lesson 2: NVIDIA independently converged on Prax's exact stack (sandbox + local-open-model + MCP + governed tools). Lesson 3: borrow MCP-serves-its-own-docs + a named lifecycle layer.
- [Agentic To-Do Flows](agentic-todo-flows.md) — §20-§27: Where explicit plan+execute loops beat alternatives (and where they don't)
- [Prax Changes From To-Do Research](prax-changes-from-todo-research.md) — Proposals doc mapping the to-do research findings to concrete Prax changes with priorities and sizes
- [ARA — Agent-Native Research Artifacts](ara-research-artifacts.md) — Reference note on Orchestra Research's four-layer research-artifact format (logic/src/trace/evidence + Live Research Manager). Not a roadmap item; flags two adjacent ideas to keep in mind (structured layout for research-mode library spaces; surfacing dead ends in trace summaries).
- [Agentic Resource Discovery (ARD)](agentic-resource-discovery.md) — Reference note on Google's ARD spec (publish/discover/verify AI capabilities across the web via `ai-catalog.json` at a well-known domain path + domain-ownership trust). Code-grounded verdict: **document, don't adopt now** — ARD targets cross-org "agentic web" discovery, a problem this single-user harness doesn't have; its trust half is redundant with (and weaker than) Prax's imported→HIGH + capability-gateway + glass-sandbox model. Evaluating it surfaced two latent gaps worth fixing regardless: no SSRF egress filtering, and a behavioral-not-hard plugin activation gate.
- [Open Knowledge Format (OKF)](open-knowledge-format.md) — Reference note on Google Cloud's OKF (knowledge as a directory of markdown files + YAML frontmatter, cross-linked into an implicit graph; "the format is the contract"). Maps OKF against Prax's two knowledge stores — the Library is already ~80% OKF; the Neo4j concept graph is OKF-shaped but file-less. Verdict: assimilate OKF as an **interchange/portability format**, not as an internal model — Prax's vector-hybrid graph is strictly richer. **Shipped**: an OKF export/import bridge (`okf_bridge.py` + `knowledge_export_okf`/`knowledge_import_okf`) makes the concept graph round-trippable to portable markdown bundles.
- [Reliable Agentic AI Systems — Bayer/PRINCE case study, mapped to Prax](reliable-agentic-systems-bayer.md) — Reference note on Kulkarni/Thoughtworks' martinfowler.com case study (the "context engineering + harness engineering" reliability thesis). An evidence-checked alignment matrix of all ~41 article principles against Prax's *shipped code* (every row verified file:line; aspirational docs excluded): ~19 strong, ~12 partial, 7 genuine gaps, 3 pharma-specific N/A. The article largely validates Prax's architecture; the verified inspired changes are cross-provider LLM fallback, automated/continuous evals, durable checkpoint+resume, within-turn recovery-context injection, and orchestrator-prompt selectivity.
- [Lift — Schema-Constrained Document Extraction (Datalab)](lift-document-extraction.md) — Reference note on `datalab-to/lift`, a 9B (Qwen-3.5-based) VLM that extracts **typed JSON from PDFs/images against a caller-supplied JSON Schema** via schema-constrained decoding (a gap Prax's read/convert stack — Jina `url_reader` + opendataloader `pdf_service` — doesn't fill). **Verdict: document, don't bundle.** License is **Modified OpenRAIL-M** (weights restricted: free for research/personal/sub-$5M, "can't compete with our API"; code Apache-2.0), so it stays an **opt-in, self-hosted** path (fits the existing `VLLM_BASE_URL` / GPU-sandbox rails) for non-commercial users — never a Prax dependency or default.
- [Building Our Own Document-Extraction Model](diy-document-extraction-model.md) — Design/build plan for a commercial-friendly equivalent of Lift (PDF/image + JSON Schema → guaranteed-valid typed JSON), reusing Prax's fine-tune + vLLM + goldens infra. Key insight: the **validity guarantee is decode-time (constrained decoding), free on any permissive base with zero training** — training only buys value-accuracy. Phased: **Phase 0** constrained-decode MVP on Qwen3(-VL) (Apache-2.0) via the existing vLLM rails + a flag-gated `document_extract` spoke (days, no training); **Phase 1** LoRA SFT on synthetic/permissive data for accuracy (reuses `finetune_service`); **Phase 2** optional vision VLM + GRPO on field-accuracy. ≈70% scaffolded in Prax today; honest "build vs. just constrained-decode a frontier API" take included. Tracked via the `document_extract` golden.
- [PixelRAG — Visual (Screenshot) RAG](pixelrag-visual-rag.md) — Reference note on `StarTrail-org/PixelRAG`: retrieval over **page images** (render→tile→embed with LoRA-tuned Qwen3-VL-Embedding-2B→FAISS→reader) so tables/charts/layout that HTML→text parsing destroys stay answerable. **Verdict: adopt-candidate** (not just reference) — **Apache-2.0**, fills Prax's text-only-retrieval gap, and reuses our existing CDP/Playwright render path; would be a flag-gated, self-hosted "visual RAG" mode with a new image index. Shares the Qwen3-VL backbone with the DIY-extractor Phase 2.
- [Unlimited-OCR — long-horizon document OCR (Baidu)](unlimited-ocr.md) — Reference note on `baidu/Unlimited-OCR` (DeepSeek-OCR lineage): a VLM for **"one-shot long-horizon parsing"** — a whole multi-page PDF/image set as *unified* input → faithful structured text (tables/layout/cross-page), served via an OpenAI-compatible SGLang endpoint. **Verdict: adopt-candidate** — fills the **VLM-OCR gap** Prax's text-extraction read stack (`url_reader.py` Jina + `pdf_service.py`) leaves on scanned/handwritten/table-heavy/multi-page docs (the same structure-loss PixelRAG flagged). The load-bearing detail: it's **MIT**, so the capability class that was a non-starter as Lift (OpenRAIL-M restricted) becomes a first-class, commercial-clean, self-hosted path. Composes as the **OCR front-end *into*** the #14 schema-extraction plan (PDF→faithful text→constrained-decode→typed JSON); distinct from PixelRAG (transcription vs. retrieval). Tracked from #14; validate quality vs. the current stack before defaulting on.
- [Two Qwen3 models on one DGX Spark](two-qwen3-on-one-spark.md) — Reference note (Devashish Meena) on co-hosting a heavy + a fast model on **one** GPU box via two vLLM backends behind a LiteLLM proxy. Load-bearing lessons: vLLM (not Ollama) for memory partitioning; **`gpu_memory_utilization` is a fraction of TOTAL VRAM, not free** (sum < ~0.95); load-big-first→measure→size-small; FP8 fits an 80B in ~120 GiB. Prax tie-in: one OpenAI-compatible proxy in front of N vLLM backends = a single `VLLM_BASE_URL` serving a heavy + fast model, the local analog of low/medium/high tiers. Feeds [`../guides/local-vision.md`](../guides/local-vision.md) + [`../guides/cloud-gpu.md`](../guides/cloud-gpu.md).
- [Agent Skills — the authoring discipline](agent-skills-discipline.md) — Reference note on Anson Biggs' *"You're Probably Using Agent Skills Wrong"*. The article's value isn't the `SKILL.md` file shape (Prax already self-authors tools via `plugin_write` and already does progressive disclosure) but the **authoring discipline**: an artifact should be born from a **gap discovered by actually solving a problem**, never generated speculatively for an unsolved one. Code-grounded verdict: **reference + principle to reinforce; one adopt-candidate** — Prax is ~70% aligned for *code* and *failures*, but has two real gaps: (1) **success-side, knowledge-not-code capture** (every learning loop keys on failure or graduates to a Python plugin — no lightweight "here's the hard-won procedure" artifact; `metacognitive.record_success` authors nothing), and (2) **recall by task-similarity**. Plus a transferable guardrail (don't crystallize skills/tools for unsolved problems). Tracked as IDEAS_BACKLOG #17 + the `skill_capture_reuse` golden; gated on resolving overlap with the Library / self_tools / memory before any build.
- [Provider-independence & the Fable/Mythos export-control case](provider-independence-export-control.md) — Why cross-provider resilience isn't abstract: the **2026-06-12** US export-control pull of Anthropic's **Fable 5 / Mythos 5** (a model can vanish by government action, not just rate-limit). **Shipped from this review:** terminal-failure provider **denylist + user notification** in `llm_fallback.py` (permanent-removal handling vs. the old transient-only failover), plus the still-open learned-routing direction (wire the dormant `tier_bandit.py`, ATLAS-shaped — tracked as #18). **Correction:** this note previously cited Sakana **Fugu** (Conductor/Trinity) as external *validation*; that framing was **pulled** after a community note flagged Fugu's benchmark comparisons as unfair (self-reported Fugu vs. published vendor scores, different scaffolds — SWE-Bench-class scaffold swings of 10–20 pts). The export-control event + shipped denylist + the bandit-wiring idea stand independently; Fugu is no longer cited as evidence.
- [TeamWork vs. Microsoft Loop](teamwork-vs-microsoft-loop.md) — **(canonical copy moved to the TeamWork repo: `teamwork/docs/comparisons/microsoft-loop.md`; this is a pointer stub.)** Comparison of Prax's UI (TeamWork) with Microsoft Loop. They look adjacent ("a workspace for a team") but sit on opposite sides of one axis: **Loop is a surface for humans to co-create content (Copilot assists); TeamWork is a surface for humans to collaborate with AI agents that do work (you supervise).** Loop wins on real-time co-editing, portable components, and M365 governance; TeamWork wins on agents-as-teammates, watching-the-work-happen (terminal/browser/desktop screencast + execution-graph + LGTM observability), and being open/self-hostable/sovereign. **Verdict: not competitors; one idea worth borrowing** — Loop's *portable, live-syncing component* (tracked as cross-cutting item #21).
- [Harness Engineering](harness-engineering.md) — §28-§31: The environment is the agent. SWE-agent's ACI (64% lift from interface design alone), Anthropic's long-running harness (initializer + coding agent, `feature_list.json`, `claude-progress.txt`, `init.sh`, Puppeteer E2E), OpenAI Codex's 1M-LOC zero-manual-code run (repository as system of record, progressive disclosure via `AGENTS.md` map + structured `docs/`, worktree isolation, CDP + queryable observability, layer-enforcing linters, cleanup bots, minimal merge gates), and the awesome-agent-harness 7-layer taxonomy. Maps each finding to Prax's current state and flags the gap between Prax's strong interactive tooling and the reproducible-session harness patterns it's missing.
- [Learning to Theorize the World (NEO) — transfer-not-memorize, formalized](learning-to-theorize.md)
- [Safety from Honesty in a Disinterested AI Predictor (Bengio et al.)](disinterested-predictor-safety.md) — the "Scientist AI" agenda: a **disinterested predictor** that earns no reward from deployment effects and treats goal-expressions in text as **patterns to explain, not adopt**; accuracy constraints make coordinated deception costly. **Document + principle, not a build** (theoretical, no benchmarks) — but the strongest formal grounding yet for Prax's **disinterested self-regen loop** (verifier/overseer OUTSIDE the editable surface), **injection resistance** (injected goals are data, not commands), and the **un-gameable verifier**. Transferable rule: keep the self-improving component a disinterested proposer; all rewards live in the external un-gameable verifier. — Reference note on Baek et al.'s cognitive-ML paper: understanding ≠ prediction; it's inferring a compact **executable theory** (composition of reusable primitives) from a few **support** pairs and verifying it **transfers** to held-out **query** inputs (the OTIB benchmark). **Verdict: adopt ONE principle, track the methodology, not a build.** It formalizes Prax's **never-spike / transfer-not-memorize** discipline — and its sharpest finding, *"high self-explainability does NOT guarantee transfer,"* is a direct warning for maker≠checker: a well-reasoned rationale isn't evidence of generalization, only **held-out transfer** is (keep the verifier deterministic). Concrete adopt: an **MDL/Occam bias** for the self-regen loop (among improving patches, prefer the simplest) — composes with `loop_cost_per_accepted_change`.
- [Inherently interpretable LLMs (Guide Labs) — the API-level attribution unlock](inherently-interpretable-models.md) — Reference note on Guide Labs' *Scaling Inherently Interpretable Language Models* (Steerling-8B): interpretability built INTO training (hidden state = known concepts + unknown + residual, linear head → exact per-concept contributions), exposing **faithful-by-construction** input attribution + concept attribution + training-data retrieval as first-class OUTPUT. **Verdict: track + internalize one principle, don't adopt (proprietary/closed weights, ~51.6% vs 60%+ at 8B).** The unlock: inherently-interpretable models put faithful attribution at the **API/output level**, breaking this lane's "Prax can't read activations" constraint — a mechanistic signal for grounding, injection detection (composes with the just-shipped **lethal-trifecta guard**), and harder-to-game **self-regen #29** fitness. Adopt IF an open one appears. Principle to use NOW: their "share of prediction flowing through interpreted concepts vs residual" is an output-level grounding metric Prax can approximate today.
- [LLM behavior — narrative/emotion as geometric trajectories (Goodfire)](llm-emotion-manifolds.md) — First entry in the **LLM behavior & interpretability** lane (below). Goodfire's "Meandering on Manifolds" shows an LLM reading a story traces a **geometric trajectory** along an emotion **manifold** (valence–arousal), maintaining a **dynamically-updating belief state** — and that what you can *prompt* it to report matches its *internal* state. **Verdict: reference + principles, not a build** (Prax consumes an API, no activation access). Three harness principles: (1) **context *order* is load-bearing** (early framing constrains later reading → deliberate context construction); (2) the model has a **trackable evolving state, so drift is detectable** — validating Prax's output-level proxies (semantic-entropy/hallucination guards, trace introspection, verify-loop; composes with #19, #22); (3) **introspection is partly grounded but must stay verifier-gated**.

## LLM behavior & interpretability — why model internals shape the harness

A standing lane for research into **how models actually behave/represent things
internally**, read for its **harness implications**. The premise: the harness is
built *around* a model whose internal dynamics are a **design input**, not a black
box — so findings about belief states, representation geometry, context-order
effects, introspective faithfulness, and failure geometry should inform Prax's
context engineering, reliability guards, and verification design. Prax can't read
activations (it's an API consumer), so these land as **principles + validation**,
realized through output-level proxies — unless a self-hosted backend ([#20](../IDEAS_BACKLOG.md))
later exposes hidden states.

- [Goodfire — narrative/emotion manifolds](llm-emotion-manifolds.md) — belief
  states as geometric trajectories; context-order effects; grounded-but-verify
  introspection. *(start here)*
- [Pangram Space — AI-text detector internals](pangram-detector-interpretability.md) —
  interpretability of an AI-text *detector* (human-vs-AI separates by layer 24;
  model family emerges at ~91%; "humanizing" leaves a signature). Thin *direct*
  harness value (no activation access), but it earns its place by **bridging to
  interoperability**: AI content is reliably detectable, so Prax should aim to be
  *verifiably attributable* (content provenance) rather than undetectable — see
  [`../architecture/interoperability.md`](../architecture/interoperability.md).
- **Parameter decomposition — interpretable fine-tune *control*** (Goodfire:
  [param-decomp toolkit, **MIT**](https://github.com/goodfire-ai/param-decomp);
  SPD [arXiv 2506.20790](https://arxiv.org/abs/2506.20790) + VPD/"exploration
  fine-tuning",
  [LessWrong](https://www.lesswrong.com/posts/ieoWstubDQWLrMnhH/exploration-fine-tuning-with-parameter-decomposition)).
  Decompose weights into interpretable rank-1 subcomponents, then fine-tune only
  *scalar masks* to **amplify / suppress / invert existing circuits** (not add
  params like LoRA) — surgical, interpretable, extremely data-efficient (~4 tokens
  vs 32+), minimal collateral damage. **Sits at the interpretability ↔ fine-tuning
  seam.** Honest Prax fit: a **concrete adopt-candidate** (it's a usable MIT library
  you point at your own model) **only** for surgical *suppression/control* of a
  **local** fine-tuned model — **not** the API policy (no weights) and **not** #14's
  *add-capability* extraction goal (decomposition reweights, it can't teach new
  skills). The complement to LoRA, for the day Prax needs to kill a failure mode or
  drop a capability without retraining. (Goodfire — same lab as the manifolds note.)
- Behavior-adjacent docs already in this collection, re-read through this lens:
  [Active Inference](active-inference.md) (agents as belief-state maintainers /
  self-verification), [Grounding](grounding.md) (anti-hallucination / evidence
  floors), and [Error & Metacognition](error-metacognition.md) (failure profiles,
  multi-perspective recovery). The shipped **hallucination-guard / semantic-entropy**
  metrics are the output-level proxy these internals justify.
- *Add related sources here as they come up — interpretability, mechanistic
  analyses, behavioral evals — each with the same "what does this change about the
  harness?" framing.*

## Key Takeaways

1. **Planning is not optional** — it's the single highest-leverage intervention for multi-step tasks.
2. **Verification beats generation** — checking work is cheaper and more reliable than generating it perfectly the first time.
3. **Persistence prevents drift** — re-inject state every turn; don't trust the context window to remember.
4. **Bound your parallelism** — diminishing returns kick in hard after ~4 concurrent sub-agents.
5. **Make the honest path the smooth path** — architectural enforcement (plan requirements, verification gates) works better than prompt-only instructions.
6. **Fewer tools, better choices** — tool selection accuracy collapses past 20–50 tools. Use hub-and-spoke delegation or on-demand tool search to keep each agent's tool set focused.
7. **Diverse reviewers improve quality** — different models/providers in a review loop outperform same-model self-critique by 9+ percentage points.
8. **Put security boundaries at the OS level** — in-process Python sandboxing is fundamentally fragile (the Glass Sandbox). Use subprocess/process isolation as the primary boundary, keep in-process guards as defence-in-depth, and make the framework enforce limits the plugin code cannot override.
9. **Let the data choose the model** — Thompson Sampling learns which tier works best per component, replacing guesswork with Bayesian inference.
10. **Spend compute where it matters** — difficulty-driven routing allocates expensive models to hard tasks and cheap models to easy ones, matching adaptive computation research.
11. **Never trust the model's self-reported confidence** — LLMs are systematically miscalibrated due to RLHF. Measure uncertainty extrinsically through prediction errors, behavioral variance, and token entropy. The harness must be the arbiter of confidence, not the model.
12. **Prove every mechanism earns its complexity** — log prediction errors, correlate with outcomes, A/B test epistemic gating. Remove mechanisms that don't measurably improve task completion.
13. **Benchmark externally** — internal tests validate architecture; external benchmarks (GAIA, SWE-bench) establish competitive positioning and surface blind spots.
14. **Autonomy is a continuum, not a trichotomy** — the meaningful question isn't "hardcoded vs dynamic" but "which degrees of freedom at runtime". Use an autonomy-level taxonomy: **L0** (recipe selection only), **L1** (parameterized fixed skeleton — prompts/rubric/budget dynamic, shape fixed), **L2** (validated graph composition from typed primitives), **L3** (unbounded — empirically broken, avoid). Pure dynamic composition is empirically fragile (AutoGPT-style); pure hardcoded pipelines can't cover the long tail. The winning shape across Voyager, ChatHTN, LLM-Modulo, RAP, MetaGPT, and Anthropic's orchestrator-workers is: **deterministic backbone + library of proven units + LLM as gap-filler when retrieval misses + hard verifier on output**. Prax today is L0 with L1 machinery built but not exposed. Before escalating, **instrument the current system first** — measure where L0 actually fails (Pareto chart of missing shapes) and justify L1/L2 from evidence, not ideology.
15. **Agentic to-do flows are only conditionally superior** — they beat one-shot prompting on controlled tool-use benchmarks by a few percentage points (ReAct 71%→45%; TPTU sequential 55%→50%), but absolute performance collapses on realistic constraint-heavy planning (WebArena 14%, GAIA 15%, TravelPlanner 0.6%). Plan-visibility is double-edged — exposing the to-do list raises cognitive load and can *reduce* plan quality via miscalibrated trust. Dependency graphs beat linear to-dos by ~15 points. Externalize working memory to files instead of stuffing the context window. Treat tool outputs as a to-do-injection attack surface; every agent-added subtask should trace back to a user goal. Plausible plans mislead users more than ugly correct ones do.
